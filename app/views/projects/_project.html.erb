<% full_page ||= false %>
<% css_classes ||= "m-2" %>
<% link_target = project.url if full_page != false %>
<% link_target ||= project_path(project) %>
<div class="card <%= css_classes %>">
  <% if project.image.attached? %>
    <a href="<%= link_target %>">
      <%= image_tag project.image, class: 'card-img-top' %>
    </a>
  <% end %>
  <div class="card-body">
    <div class="d-flex w-100 justify-content-between">
      <a href="<%= link_target %>">
        <h3 class="card-title"><%= project.name %></h3>
      </a>
    </div>
    <h5 class="card-subtitle mb-2 text-muted"><%= raw project.summary %></h5>
    <% if full_page != false %>
      <p><%= raw project.description %></h1>
    <% end %>
  </div>
</div>
